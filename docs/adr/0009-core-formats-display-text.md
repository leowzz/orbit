---
status: accepted
---

# Core formats display text

Core projects structured Observations into Device Model-specific display slots with final text and limited emphasis metadata. The OLED 128x32 contract has exactly three slots: Primary, Secondary, and Footer. Nodes choose fixed font, truncation, and pixel placement but do not format currency, abbreviate values, accept arbitrary line counts, or receive cloud-rendered pixel frames.
