agent:
  id: agent-local
  host_id: host-local

core:
  id: core-local

mqtt:
  url: mqtts://localhost:8883
  tls:
    ca_file: certs/ca.pem
    cert_file: certs/client.pem
    key_file: certs/client.key

logging:
  level: info

